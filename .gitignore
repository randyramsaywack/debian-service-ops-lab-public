.DS_Store
*.retry
*.log

# Local inventory or secrets should stay out of git unless intentionally sanitized.
*.vault
*.key
*.pem
.env

# Local CodeMachine harness state (logs, memory, prompts, agent inputs).
.codemachine/
